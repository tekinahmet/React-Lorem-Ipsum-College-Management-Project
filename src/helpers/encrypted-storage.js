/* 
import { EncryptStorage } from "encrypt-storage";

const encryptKey = import.meta.env.REACT_APP_ENCRYPT_KEY;
console.log(encryptKey);
if (!encryptKey) {
  throw new Error("Encrypt key is not provided.");
}
const encryptStorage = new EncryptStorage(encryptKey);


export const setLocalStorage = (key, value) => {
  encryptStorage.setItem(key, value);
};
export const getLocalStorage = (key) => {
  return encryptStorage.getItem(key);
};
export const removeLocalStorage = (key) => {
  encryptStorage.removeItem(key);
};
 */




//  import { EncryptStorage } from "encrypt-storage";

// const key = import.meta.env.REACT_APP_ENCRYPT_KEY;

// const encryptStorage = new EncryptStorage(key);

// console.log(key);
// export const setLocalStorage = (key, value) => {
//  encryptStorage.setItem(key, value);
// };
// export const getLocalStorage = (key) => {
//  return encryptStorage.getItem(key);
// };
// export const removeLocalStorage = (key) => {
// encryptStorage.removeItem(key);
// };
